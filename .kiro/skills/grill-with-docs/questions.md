# Domain Questions

- What is the expected latency budget for this feature?
- Does this touch any PII or regulated data?
- Is this change reversible without a migration?
- What happens if this feature is partially deployed (e.g. one service updated, another not)?
- Are there existing patterns in the codebase that this should follow or deliberately diverge from?

